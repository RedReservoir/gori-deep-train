## Data Loading

The `settings/data_loading.json` JSON file defines logging settings. Schema:

```json
{
  "step_min_items": <int>
}
```

  - `step_min_items`: Number of items necessary to complete a step in the train and eval loops.
