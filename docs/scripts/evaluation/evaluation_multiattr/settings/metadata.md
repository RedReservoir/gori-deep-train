# Metadata

The `settings/metadata.json` JSON file defines category metadata settings. Schema:

```json
{
  "multiattr_subset_name": <str>
}
```

  - `multiattr_subset_name`: Multi-attribute metadata subset name.
